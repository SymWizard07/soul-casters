Start the Server.jar file if hosting the game.
Start the Client.jar file and enter the host's IP to connect in the GUI.

To configure the database, run the script in accounts.sql logged in as "student" with the password "hello" under the database "student_space"

JUnit testing classes can be found in src/test/java